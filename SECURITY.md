# Security Policy

## 支持范围

当前仍处于早期开发阶段，只维护 `main` 分支的最新版本。

## 报告安全问题

请不要在公开 Issue 中提交以下内容：

- 可利用的内存安全或权限提升漏洞
- 包含 Cookie、Token、密码或内部地址的抓包文件
- 能识别个人或机构的真实网络记录

请通过 GitHub Security Advisories 的私密报告功能提交。

## 数据处理原则

PacketLens 的设计原则：

1. 捕获数据默认只保存在本机。
2. 不提供默认遥测。
3. 不自动启用 TLS 解密或 HTTPS 中间人代理。
4. 原始 payload 和导出文件被视为敏感数据。
5. Npcap 仅从本机动态加载，不在仓库中重新分发。
6. 后续特权抓包服务采用最小权限和受限 Named Pipe 协议。

## 使用授权

只能监控你拥有、管理或获得明确授权的设备和网络。用户需要自行遵守所在地法律、组织政策和数据保护要求。
